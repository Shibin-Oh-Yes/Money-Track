import 'package:flutter/material.dart';

class CashElevatedBtn extends StatelessWidget {
  const CashElevatedBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width:MediaQuery.of(context).size.width*.4,
      height: 20,
      
    );
  }
}